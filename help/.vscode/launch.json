{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/help-promise/target/server.js",
            "outFiles": [
                "${workspaceFolder}/help-promise/target/**/*.js"
            ]
        }
    ]
}